# Nombre de la aplicación (Se mantiene el tuyo)
spring.application.name=projectD1Campuslands

# Configuración de la base de datos (Actualizada con la URL nueva y tu contraseña '123')
spring.datasource.url=jdbc:mysql://localhost:3306/logitrak?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de Hibernate y SQL (Actualizado según las instrucciones)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.sql.init.mode=always

# Configuración de ERRORES (Se mantiene el tuyo)
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false